{
  "name": "Mastery",
  "description": "An HTML5 platform game where you take on the role as a wizard fighting the undead.",
  "dependencies": {
    "promise": ">= 6.0.1 < 7.0.0",
    "socket.io-client": ">= 1.2.1 < 2.0.0"
  },
  "devDependencies": {
    "browserify": ">= 6.2.0 < 7.0.0",
    "uglify-js": ">= 2.4.0 < 3.0.0",
    "nodeunit": ">= 0.9.0 < 1.0.0"
  },
  "scripts": {
    "test": "make test"
  }
}
